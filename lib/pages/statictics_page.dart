import 'package:flutter/material.dart';

class StaticticsPage extends StatelessWidget {
  const StaticticsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Statictics page!'),
      ),
    );
  }
}
